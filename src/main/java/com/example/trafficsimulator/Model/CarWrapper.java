package com.example.trafficsimulator.Model;

public class CarWrapper extends VehicleWrapper {
    public CarWrapper(int i) {
        super(i);
    }

    public void load(Car car) {
        super.load(car);
    }
}
